def publish_document_uploaded(document_id: int, user_id: int):
    """
    Placeholder cho Kafka / Redis sau này.
    """
    print(
        f"[EVENT] DOCUMENT_UPLOADED | document_id={document_id} | user_id={user_id}"
    )
